meta {
  name: Create project
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projects
  body: json
  auth: inherit
}

body:json {
  {
    "name": "test-mon-projet",
    "description": "Projet de test"
  }
}

script:post-response {
  bru.setEnvVar("projectId", res.body.id);
}
