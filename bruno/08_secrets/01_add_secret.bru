meta {
  name: Add secret
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/secrets
  body: json
  auth: none
}

body:json {
  {
    "name": "DB_PASSWORD"
  }
}

script:post-response {
  bru.setEnvVar("secretId", res.body.id);
}
