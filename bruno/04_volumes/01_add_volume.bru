meta {
  name: Add volume
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/volumes
  body: json
  auth: none
}

body:json {
  {
    "name": "data",
    "driver": "local"
  }
}

script:post-response {
  bru.setEnvVar("volumeId", res.body.id);
}
