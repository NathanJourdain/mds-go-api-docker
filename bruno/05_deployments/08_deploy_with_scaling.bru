meta {
  name: Deploy project with scaling
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/projects/{{projectId}}/deployments
  body: json
  auth: none
}

body:json {
  {
    "name": "v1-scaled",
    "secret_override": {
      "DB_PASSWORD": "prod_s3cr3t"
    },
    "scale": {
      "web": 2
    }
  }
}

script:post-response {
  bru.setEnvVar("deploymentId", res.body.id);
}
